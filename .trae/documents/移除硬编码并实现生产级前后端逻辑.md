## 问题分析

### 前端硬编码位置

1. **MessageList.tsx:48-53** - 示例品牌按钮（观夏、野兽派、蕉内、瑞幸咖啡）
2. **ChatPanel.tsx:157-166** - 模拟响应中的硬编码品牌信息（品类、定位、竞品）
3. **ChatPanel.tsx:86-197** - 整个 simulateAgentResponse 函数是模拟逻辑

### 解决方案

#### 1. 创建配置系统
- 创建 `frontend/src/config/brands.ts` - 存储示例品牌配置
- 支持从后端 API 获取热门品牌

#### 2. 修改 ChatPanel.tsx
- 移除 `simulateAgentResponse` 模拟函数
- 集成 `useWebSocket` hook
- 通过 WebSocket 发送消息到后端
- 监听 WebSocket 事件更新 UI

#### 3. 确保后端 WebSocket 实现完整
- 检查 `app/api/v1/websocket.py` 实现
- 确保 Agent 调用链路完整
- 确保 LLM 调用正常工作

#### 4. 实现步骤

**前端修改：**
1. 修改 `MessageList.tsx` - 将硬编码品牌改为从 props 传入
2. 修改 `ChatPanel.tsx` - 使用真实的 WebSocket 通信
3. 创建 `config/brands.ts` - 品牌配置

**后端检查：**
1. 检查 WebSocket 端点实现
2. 检查 Agent 调用链路
3. 检查 LLM 配置

**测试：**
1. 启动前后端服务
2. 测试完整对话流程
3. 验证 Agent 和 LLM 调用

请确认此计划后，我将开始执行具体的代码修改。