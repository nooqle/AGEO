## 测试目标
验证前端重构结果，与Manus、AnyGen设计对比，确保达到AI Native交互标准。

## 当前服务状态
- **后端** (terminal_4): Python服务在端口8003运行中
- **前端** (terminal_5): Next.js开发服务器在端口3002运行中

## 测试步骤

### 1. 重启前后端服务
由于之前修改了大量代码，需要重启服务确保变更生效：

**后端重启：**
```bash
# 停止当前服务，重新启动
cd d:\AGEO\aeo-platform\backend
python -m uvicorn app.main:socket_app --host 0.0.0.0 --port 8003 --reload
```

**前端重启：**
```bash
# 清理.next缓存，重新构建
cd d:\AGEO\frontend
Remove-Item -Recurse -Force .next
npm run dev
```

### 2. 浏览器截图对比

#### 2.1 首页效果验证
- 截图当前首页
- 检查：深色主题是否统一、EmptyState样式

#### 2.2 创建新分析流程
- 点击"新建分析"
- 输入品牌名称（如"瑞幸咖啡"）
- 截图整个交互过程

#### 2.3 TPAOR时序验证
- 观察"思考→规划→行动→观察→回复"各阶段
- 验证：
  - 阶段是否自动展开
  - 1.5秒后是否自动折叠
  - MiniProgress是否显示正确
  - 颜色是否符合设计规范

#### 2.4 与Manus对比
Manus特点：
- 简洁的深色界面
- 流式输出为主
- Progress小巧低调
- TPAOR渐进式披露

需要验证我们的实现是否达到同样效果。

#### 2.5 与AnyGen对比
AnyGen特点：
- Canvas动态展示
- 实时更新
- 可视化Agent执行

需要验证我们的Canvas和AgentCallIndicator是否符合预期。

### 3. 验证清单

#### 视觉验证
- [ ] 整个界面统一深色，无浅色元素
- [ ] 文字层级清晰（白色主文字，灰色次要文字）
- [ ] TPAOR各阶段颜色正确（紫/蓝/橙/绿/靛蓝）
- [ ] MiniProgress小巧，不占空间

#### 交互验证
- [ ] 用户发送消息后，TPAOR阶段自动推进
- [ ] 每个阶段展开1.5秒后自动折叠
- [ ] 可以随时点击展开查看详情
- [ ] LLM回复流式输出

#### 功能验证
- [ ] WebSocket连接正常
- [ ] Agent调用显示正确
- [ ] Progress步骤更新正确
- [ ] 无控制台错误

### 4. 截图对比
将截图保存到 `d:\AGEO\Doc\TestResults\` 目录，与参考设计对比。

### 5. 问题修复
如果发现与Manus/AnyGen设计有差距，立即修复。

请确认此计划，确认后我将立即执行测试。