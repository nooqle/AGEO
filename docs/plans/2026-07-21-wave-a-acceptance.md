# Wave A 验收标准（开工锁定 · 开发后对照）

日期：2026-07-21  
状态：**验收标准已锁定；实现已提交工程校验，F 项待手点补全**  
关联总路线：`docs/plans/2026-07-21-blueprint-status-and-next.md`

---

## 0. Wave 协议（全 Wave 通用）

| 时机 | 动作 |
|---|---|
| **Wave 开始** | 写本类文档：范围、非目标、**可勾选验收项**、命令级回归 |
| **Wave 开发中** | 只做范围内事项；范围外记入下一 Wave，不Silent 膨胀 |
| **Wave 结束** | 逐条勾选；写 `结果`（✓/✗ + 证据）；全 ✓ 才宣称 PASS |
| **未全过** | 标 FAIL / PARTIAL，列缺口，禁止口头「差不多完了」 |

Wave B / C **必须**在各自开工日另开 `wave-b-acceptance.md` / `wave-c-acceptance.md`（可复制本模板），不得沿用 A 的清单充数。

---

## 1. Wave A 范围

**做：**

- 配方 v0 增强：空态、跑完建议另存、切换弱提示  
- 视觉：去紫、编排区合一、CTA/密度收敛  
- 拆分：配方+编排合并为单组件；Canvas 壳行数护栏  

**不做：**

- Orch / a5 巨石拆分  
- 意图推荐、主 Chat 接线  
- 黑盒学习、静默改默认拓扑  
- 分支总时限等审查 P2 残留  

---

## 2. 功能验收（必须全过）

| ID | 验收项 | 怎么验 | 结果 |
|---|---|---|---|
| F1 | 无配方时出现**空态**：有说明文案 + 主按钮导向「存为配方」 | 清空配方列表后进生产线 | ⬜ |
| F2 | 可将当前生产线**另存**为品牌级或组织级配方 | 另存后列表出现，带 scope 标签 | ⬜ |
| F3 | **切换配方**直接替换拓扑，无二次确认弹窗 | 两套配方互切；图与 GET topology 一致 | ⬜ |
| F4 | 可**删除**配方（删除本身可有确认） | 删除后列表消失 | ⬜ |
| F5 | **运行即开跑**：点开始后进入执行，不进 `waiting_scope_confirmation` 二道闸 | 起跑后 status 非 waiting 确认闸 | ⬜ |
| F6 | 规则/预设编译预览 → 确认应用仍可用 | 「跳过豆包」预览后应用 | ⬜ |
| F7 | run 为 `completed` 时出现**存为配方**入口 | 完成态 run 下可见 CTA | ⬜ |
| F8 | 切换配方后有**当前配方**弱提示（文案或角标） | 切换后可见名称 | ⬜ |

---

## 3. 视觉 / 交互验收（必须全过）

| ID | 验收项 | 怎么验 | 结果 |
|---|---|---|---|
| V1 | 分析节点徽章**无** violet/purple/`#8B5CF6` 等旧 AI 紫 | `rg` 扫 `amway-flow` + 肉眼 | ✓ 工程扫描；肉眼 ⬜ |
| V2 | 配方与编排建议在**同一编排面板**内，上下结构清晰 | 肉眼：非两块平行灰条硬叠 | ✓ 结构已合并；观感 ⬜ 待手点 |
| V3 | 主行动仍为 Evidence Teal；无大面积装饰 glow | 肉眼 + 主 CTA 类名收敛 | ✓ 去掉 amway-cta-glow |
| V4 | 类型色有角色：内容节点橙可保留，但不与紫并存 | 对照 A2.4 色表 | ✓ 代码色表 |
| V5 | 演示路径不显「设置页堆功能」 | 30 秒演示路径主观过关 | ⬜ 待手点 |

**色表（Wave A 锁定）：**

| 角色 | 用法 |
|---|---|
| Teal `--brand-*` | 主按钮、选中、高置信/品牌识别 |
| 纸面中性 | 默认表面与边框 |
| 错误红 `--error` | 失败、危险删除 |
| 石色/灰蓝（分析） | 分析节点类型（**非紫**） |
| 克制橙（内容） | 内容创作节点类型 |

---

## 4. 工程验收（必须全过）

| ID | 验收项 | 怎么验 | 结果 |
|---|---|---|---|
| E1 | `python scripts/run_3b1_suite.py` 全绿 | 命令 exit 0 | ✓ 81 passed |
| E2 | `frontend` `tsc --noEmit` exit 0 | 命令 | ✓ |
| E3 | `AmwayFlowCanvas.tsx` **&lt; 1600 行** | `Measure-Object -Line` | ✓ ~1506 |
| E4 | Canvas **只挂一层**编排面板组件，不内联配方+编译两套大段 JSX | 读 import / 结构 | ✓ `AmwayFlowOrchestrationPanel` |
| E5 | `amway-flow` 与编排面板源码无 violet/purple 硬编码 | 扫描 | ✓ 去紫后无命中 |
| E6 | 变更涉及中文无 `???` 腐蚀 | 抽查关键文件 | ✓ |
| E7 | `god-file-split-backlog` / 总路线勾选与 Wave A 一致 | 文档 | ✓ |

---

## 5. 非目标核对（必须仍为「未做」）

| 项 | 结果 |
|---|---|
| 未开拆 `orchestrator_node.py` | ⬜ |
| 未做意图自动推荐配方 | ⬜ |
| 未做静默改默认拓扑 | ⬜ |

---

## 6. Wave A 总裁决

| 字段 | 填写 |
|---|---|
| 执行人 | |
| 完成日 | |
| 功能 F1–F8 | ⬜ 全过 / ⬜ 未过 |
| 视觉 V1–V5 | ⬜ 全过 / ⬜ 未过 |
| 工程 E1–E7 | ⬜ 全过 / ⬜ 未过 |
| 非目标 | ⬜ 守住 |
| **总裁决** | ⬜ **PASS** / ⬜ **PARTIAL** / ⬜ **FAIL** |
| 证据 | session-log 路径、命令输出摘要 |

**PASS 条件：** F 全过 ∧ V 全过 ∧ E 全过 ∧ 非目标守住。  
**PARTIAL：** 仅次要项（如 V5 主观）未过且已记缺口。  
**FAIL：** F/E 任一项硬失败。

---

## 7. Wave B / C 占位

- Wave B 开工日：创建 `docs/plans/YYYY-MM-DD-wave-b-acceptance.md`，至少含 B1–B5 可测条目。  
- Wave C 开工日：创建对应 acceptance，**C4 巨石拆分**单独列工程验收（设计稿 diff、模块边界、禁止混功能 PR）。  
