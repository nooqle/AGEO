# Wave Switch — 壳切换 / M2（Dashboard+Chat 外壳退役 · Chat 降级）

日期：2026-07-23  
状态：**口径已锁 · 待开工**  
前置：M1 amwaychina 形态成立（Wave A–Q / 完善轨 PASS）  
对照：蓝图 §1.3 抛弃 Dashboard+Chat 外壳；引擎/工具保留  

---

## 0. 双必达中的位置

| ID | 内容 | 本波 |
|---|---|---|
| **M1** | amwaychina OK | 守住；本波不回退能力 |
| **M2** | 全切新设计 | **本波唯一交付目标** |

**Chat 定案：降级（用户 2026-07-23 确认）**

- **是：** 编译器 + 辅入口（改图/配方预览确认、可选开跑、证据/轻问答）  
- **否：** 隐式 plan 的黑盒主分析舞台；Dashboard 默认灌进全流程 Chat 长跑  

---

## 1. 目标主路径（唯一对外叙事）

```
登录/选品牌 → /amwaychina 生产线
  →（可选）改图 / 套配方 / Chat 编译确认
  → 生产线「开始运行」
  → 看证据（圈层/词库/报告）
  →（可选）存配方
```

---

## 2. 范围

### 做（建议分期，可单独 PASS 子波）

| ID | 事项 | 完成定义 |
|---|---|---|
| **S1** | 默认落地 | 主导航/登录后默认进新壳（或品牌→生产线），不默认 Dashboard |
| **S2** | 起跑单一真相 | 主分析/圈层 run 的产品推荐路径 = 生产线开始运行 |
| **S3** | Chat 降级 | 入口与文案定位为「改生产线/配方」；主分析意图引导回生产线；不恢复「Chat 即主舞台」 |
| **S4** | IA / 文案 | 对外只讲：生产线、配方、运行、证据；去掉双主路径说法 |
| **S5** | 旧路由策略 | `/dashboard`、`/dashboard-v2`：redirect 到新壳 **或** 遗产只读/内测标记（开工时拍一种） |
| **S6** | 资产可达 | 词库/问题集/报告/圈层从新壳可完成，不依赖旧 Dashboard 才能看完 |

### 不做（本波）

| 禁止 | 原因 |
|---|---|
| 一夜删光 Chat/Orch 代码 | 先产品不可达/降级，再工程拆 |
| Orch 阶段 C 薄壳、第二垂直 | 非 M2 |
| 黑盒自学、静默改图 | 产品排除 |
| 把主分析重新做回纯 Chat 长跑为默认 | 与降级定案冲突 |

---

## 3. Chat 降级细则（验收语言）

| ID | 项 | 通过标准 |
|---|---|---|
| C1 | 角色 | 设置/导航中 Chat 不称「主分析」；称改生产线 / 助手 / 编译 等 |
| C2 | 主分析话术 | 「分析品牌/生成报告」类：优先引导去生产线起跑，**不**再作为唯一成功路径的默认 Chat 长跑（实现可选：强引导卡 / 仅保留逃生口） |
| C3 | 编排能力 | 保留 Wave D/Q：compile / 配方 / 确认写入 / 可选开跑 |
| C4 | 开跑 | Chat 开跑须显式二次点击；文案强调结果在生产线查看 |
| C5 | Deep-link | 从 Chat 去生产线 deep-link 清晰可用 |

---

## 4. 旧壳策略选项（开工前拍板一项）

| 选项 | 行为 | 适用 |
|---|---|---|
| **A. Redirect（推荐）** | `/dashboard` → `/amwaychina`（可带 query） | 产品默认 |
| **B. 遗产只读** | Dashboard 可进但无「新分析/进 Chat 主路径」CTA | 过渡期 |
| **C. 内测 flag** | 仅 `?legacy=1` 或员工可见 | 调试 |

默认建议：**S5 = A**，内测保留 B/C 作逃生。

---

## 5. 验收总表（本波 PASS 条件）

| ID | 项 | 结果 |
|---|---|---|
| F1 | 新用户/默认路径不经旧 Dashboard 能完成定干法→跑→证据 | ⬜ |
| F2 | 主导航无「回旧 Dashboard 主分析」并列主 CTA | ⬜ |
| F3 | Chat 降级文案与主分析引导符合 §3 | ⬜ |
| F4 | 生产线起跑仍可用；M1 能力不回退 | ⬜ |
| F5 | 旧路由策略落地且可解释 | ⬜ |
| E1 | 中文无 `???`；相关 tsc | ⬜ |
| E2 | 无静默改图/auto-run 回潮 | ⬜ |

| **总裁决** | **（未开工）** |
|---|---|

---

## 6. 与完成度的关系

| 口径 | Switch 后预期 |
|---|---|
| 3a–3c 竖切 | 基本不变（能力已有） |
| 产品命题 / 可托付 | **明显抬升**（单一主叙事） |
| 整本蓝图 | 兑现 §1.3「抛弃外壳」；不自动抬 2.4 平台化 |

---

## 7. 开工顺序建议

1. 锁 S5 旧路由选项（默认 A）  
2. S1 默认落地 + 导航  
3. S3 Chat 降级文案与主分析引导  
4. S2/S4 起跑与 IA  
5. S6 资产路径扫尾  
6. 统一手测 F1–F5  
