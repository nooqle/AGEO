# Wave C 验收标准（开工锁定 · 开发后对照）

日期：2026-07-22  
状态：**PASS（2026-07-22 手测验收）**  
前置：Wave A **PASS**、Wave B **PASS**  
关联：`docs/plans/2026-07-21-blueprint-status-and-next.md`  
实现提交：`8ab38c6`（推荐 API + UI + C4 设计稿）

---

## 0. 协议

同 Wave A/B：开工写本文件 → 开发 → 结束逐条勾选 → 全过才 PASS。

---

## 1. 范围

**做（本波可交付竖切）：**

| ID | 事项 |
|---|---|
| C1 | **意图/情境 → 配方推荐（3b-2.2 v0）**：确定性打分；每条推荐必须有**可见理由**；用户点击才套用（复用现有 apply） |
| C1.1 | 后端：`GET …/flow-recipes/recommendations`（entity 可见配方池 + 当前拓扑 + 编排事件） |
| C1.2 | 前端：编排面板展示最多 3 条建议；理由文案可见；「套用」走现网 apply |
| C1.3 | 可选 `intent` 查询参数：对名称/描述做关键词匹配（无 LLM、无静默写库） |
| C4 | **God 拆分 P2 设计稿 only**（`orchestrator_node` 等）：文档边界，**不实现大拆** |

**不做：**

- C2 主 Chat 作为编译/配方主入口（下一波）
- C3 节点级运行教训 annotation 全量
- Orch / a5 巨石**实现**拆分
- 黑盒学习、静默改默认拓扑、无理由自动推荐并套用
- 覆盖配方 Dialog 美化（已明确延后）

---

## 2. 功能验收

| ID | 验收项 | 怎么验 | 结果 |
|---|---|---|---|
| F1 | 品牌下 ≥2 条配方时，编排区出现「建议配方」且最多 3 条 | 造两条以上配方后刷新 | ✓ PASS |
| F2 | 每条建议至少 1 条中文**理由**（非空） | 肉眼 + API | ✓ PASS |
| F3 | 点击「套用」后拓扑替换成功，与列表套用一致；写编排事件 | 点建议套用 | ✓ PASS |
| F4 | 不会自动套用：仅展示，需用户点击 | 刷新不改变拓扑 version（除非用户操作） | ✓ PASS |
| F5 | 传入 `intent`（如名称关键词）时，名称命中的配方排序更靠前或理由含意图匹配 | API 或 UI | ✓ PASS |
| F6 | 无配方 / 仅 1 条且已是当前时：建议区空态或隐藏，不报错 | 边界 | ✓ PASS |
| F7 | 推荐逻辑**无 LLM 必经路径**；失败时 UI 降级为空，不阻断编排 | 断网或空池 | ✓ PASS |

---

## 3. 工程验收

| ID | 验收项 | 结果 |
|---|---|---|
| E1 | 相关单测 + `run_3b1_suite`（或等价）全绿 | ✓ PASS（推荐单测 + 拓扑相关 23 passed；开发期） |
| E2 | `tsc --noEmit` 0 | ✓ PASS |
| E3 | Canvas 壳仍 **&lt; 1600** 行；推荐 UI 不回流 Canvas | ✓ PASS（~1576；UI 在 RecipeBar） |
| E4 | 中文无损坏占位（源码扫描） | ✓ PASS |
| E5 | C4 设计稿落盘：`docs/plans/2026-07-22-orchestrator-p2-split-design.md` | ✓ PASS |
| E6 | 无新 migration（若有则文档写明 revision） | ✓ PASS（无新 migration） |

---

## 4. 非目标核对

| 项 | 结果 |
|---|---|
| 未接主 Chat 为编译主入口 | ✓ |
| 未静默改默认拓扑 / 未自动套用 | ✓ |
| 未实现 Orch 巨石代码大拆 | ✓ |

---

## 5. 总裁决

| 字段 | 填写 |
|---|---|
| 执行人 | 用户手测 + Codex 工程核对 |
| 完成日 | 2026-07-22 |
| 功能 F1–F7 | ✓ 7/7 |
| 工程 E1–E6 | ✓ 6/6 |
| 非目标 | ✓ 3/3 |
| **总裁决** | **PASS** |
| 证据 | 本机手测 OK；无 Wave C 功能 Bug；实现 `8ab38c6` |

**PASS：** F∧E 全过 ∧ 非目标守住。

---

## 6. 非阻断观察（不拦 PASS）

| 现象 | 说明 | 优先级 |
|---|---|---|
| 套用请求执行期间，旧推荐条短暂保留为 disabled，约数秒后刷新正确 | 不误套用、不重复写库；属加载态/列表未即时清空 | 低优 UX；可后续套用中清空或 skeleton |

**Wave C 功能 Bug：0。**
