services:
  aio-sandbox:
    image: ghcr.io/agent-infra/sandbox:latest
    container_name: aio-sandbox
    restart: unless-stopped
    ports:
      - "8080:8080"
    shm_size: "2gb"
    security_opt:
      - seccomp=unconfined
    volumes:
      - /opt/specta-aio-home:/home/gem
