[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    slow: long-running runtime or browser tests that are excluded from regular local gates
    timeout(seconds): documentation marker for expected long-running tests
filterwarnings =
    ignore:Please use `import python_multipart` instead\.:PendingDeprecationWarning:starlette\.formparsers
